local variables = assert(hsw_campaign.variables)
local quests = assert(hsw_campaign.quests)

quests:register_quest("hsw:ch001_stranded", {
  title = "Stranded",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})

quests:register_quest("hsw:ch001_mysterious_helper", {
  title = "Mysterious Helper",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})

quests:register_quest("hsw:ch001_scavenging", {
  title = "Scavenging",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})

quests:register_quest("hsw:ch001_element_crafting", {
  title = "Element Crafting",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})

quests:register_quest("hsw:ch001_fabrication_blueprints", {
  title = "Fabrication Blueprints",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})

quests:register_quest("hsw:ch001_wme_age", {
  title = "WME Age",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})

quests:register_quest("hsw:ch001_codex", {
  title = "Codex",

  notes = [[

  ]],
  bind = "world",

  on_start = function (entry, assigns)
    --
  end,

  on_completed = function (entry, assigns)
    --
  end,

  on_message = function (entry, assigns, message)
    --
  end,

  update = function (entry, assigns, dtime)
    --
  end,
})
